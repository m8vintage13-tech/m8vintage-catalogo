# M8 Catálogo Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Catálogo digital funcional para M8 (streetwear/vintage basketball) — catálogo público + panel admin con persistencia real en Supabase.

**Architecture:** SPA React+Vite+TS con React Router. Estilos inline con tokens centralizados. Capa de datos única (`lib/products.ts`) que usa Supabase cuando hay env; si no, cae a seed local (solo lectura) para que el catálogo renderice sin setup. Admin protegido por Supabase Auth; fotos en Supabase Storage.

**Tech Stack:** React 18, Vite 5, TypeScript, react-router-dom 6, @supabase/supabase-js 2. Sin Tailwind, sin librerías de íconos/UI.

## Global Constraints

- **Sin librerías de íconos ni de UI.** Íconos = SVG inline. Estilos = inline + `theme.ts`.
- **Sin deployment.** Solo `npm install && npm run dev`.
- Paleta exacta: INK `#0B0B0B`, BEIGE `#E3D0AC`, CREAM `#F4F1EA`, CARD `#161616`, CARD2 `#1F1F1F`, MUTED `#8A8477`, MUTED_L `#9C968A`, LINE `#2A2A2A`, SAGE `#7FA383`.
- Fuente: `Arial, Helvetica, sans-serif`. Headlines peso 900 tracking `-0.5px`. Eyebrows peso 800, `letter-spacing 0.16em–0.22em`, mayúsculas, 10–11px.
- Breakpoint responsive: 768px. Grid catálogo 2 col mobile / 3 col desktop.
- `categoria ∈ {'VINTAGE','NUEVO'}`. `precio` = integer (pesos). Formato display `$45.000`.
- Config en un solo archivo (`src/config.ts`): `WHATSAPP_NUMBER="5493415447504"`, `INSTAGRAM_USER="@m8vintage"`, `INSTAGRAM_URL="https://instagram.com/m8vintage"`, `LOCATION="Rosario, Santa Fe - Argentina"`.
- Gate automático por tarea: `npm run build` (typecheck estricto) debe pasar. Verificación visual en el browser donde se indica.

---

### Task 1: Scaffold del proyecto + tokens y helpers

**Files:**
- Create: `package.json`, `vite.config.ts`, `tsconfig.json`, `tsconfig.node.json`, `index.html`, `.gitignore`, `.env.example`
- Create: `src/main.tsx`, `src/App.tsx`, `src/vite-env.d.ts`
- Create: `src/theme.ts`, `src/config.ts`, `src/lib/format.ts`

**Interfaces:**
- Produces:
  - `theme.ts`: `export const C = { INK, BEIGE, CREAM, CARD, CARD2, MUTED, MUTED_L, LINE, SAGE }` (todos string hex); `export const font = "Arial, Helvetica, sans-serif"`.
  - `config.ts`: `WHATSAPP_NUMBER`, `INSTAGRAM_USER`, `INSTAGRAM_URL`, `LOCATION` (strings).
  - `format.ts`: `formatPrecio(n: number): string` → `"$45.000"`; `whatsappUrl(nombre: string, precioTxt: string): string`.

- [ ] **Step 1: Crear `package.json`**

```json
{
  "name": "m8-catalogo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.0"
  }
}
```

- [ ] **Step 2: Config de Vite/TS**

`vite.config.ts`:
```ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
export default defineConfig({ plugins: [react()] });
```

`tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

`tsconfig.node.json`:
```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true,
    "strict": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 3: `index.html` + `src/main.tsx` + `src/vite-env.d.ts`**

`index.html`:
```html
<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" href="/logo.png" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>M8 · Vintage & Streetwear</title>
  </head>
  <body style="margin:0">
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`src/vite-env.d.ts`:
```ts
/// <reference types="vite/client" />
```

`src/main.tsx`:
```tsx
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
```

- [ ] **Step 4: `src/theme.ts` y `src/config.ts`**

`src/theme.ts`:
```ts
export const C = {
  INK: "#0B0B0B",
  BEIGE: "#E3D0AC",
  CREAM: "#F4F1EA",
  CARD: "#161616",
  CARD2: "#1F1F1F",
  MUTED: "#8A8477",
  MUTED_L: "#9C968A",
  LINE: "#2A2A2A",
  SAGE: "#7FA383",
} as const;

export const font = "Arial, Helvetica, sans-serif";

export const eyebrow: React.CSSProperties = {
  fontWeight: 800,
  letterSpacing: "0.18em",
  textTransform: "uppercase",
  fontSize: 11,
};
```
(añadir `import React from "react";` arriba para el tipo CSSProperties.)

`src/config.ts`:
```ts
export const WHATSAPP_NUMBER = import.meta.env.VITE_WHATSAPP_NUMBER || "5493415447504";
export const INSTAGRAM_USER = "@m8vintage";
export const INSTAGRAM_URL = "https://instagram.com/m8vintage";
export const LOCATION = "Rosario, Santa Fe - Argentina";
```

- [ ] **Step 5: `src/lib/format.ts`**

```ts
import { WHATSAPP_NUMBER } from "../config";

export function formatPrecio(n: number): string {
  return "$" + n.toLocaleString("es-AR");
}

export function whatsappUrl(nombre: string, precioTxt: string): string {
  const text = `Hola! Te consulto por ${nombre} (${precioTxt})`;
  return `https://wa.me/${WHATSAPP_NUMBER}?text=${encodeURIComponent(text)}`;
}
```

- [ ] **Step 6: `.gitignore` y `.env.example`**

`.gitignore`:
```
node_modules
dist
.env
.env.local
*.log
```

`.env.example`:
```
# Copiá este archivo a .env y completá con los datos de tu proyecto Supabase.
# Si dejás esto vacío, el catálogo funciona en modo demo (6 productos de seed.ts, solo lectura).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Opcional: sobreescribe el número de WhatsApp (default 5493415447504)
VITE_WHATSAPP_NUMBER=
```

- [ ] **Step 7: `src/App.tsx` mínimo (placeholder de rutas, se completa luego)**

```tsx
import { Routes, Route } from "react-router-dom";

export default function App() {
  return (
    <Routes>
      <Route path="/" element={<div style={{ padding: 40 }}>M8 — catálogo (WIP)</div>} />
    </Routes>
  );
}
```

- [ ] **Step 8: Instalar y verificar build**

Run:
```bash
npm install && npm run build
```
Expected: install OK, `tsc -b && vite build` termina sin errores, se genera `dist/`.

- [ ] **Step 9: Commit**

```bash
git add -A
git commit -m "feat: scaffold Vite+React+TS, theme, config y helpers"
```

---

### Task 2: Capa de datos Supabase + seed (con modo demo)

**Files:**
- Create: `src/lib/supabase.ts`, `src/lib/products.ts`, `src/data/seed.ts`
- Create: `supabase/seed.sql`

**Interfaces:**
- Produces:
  - `types` (en `products.ts`): `export type Categoria = "VINTAGE" | "NUEVO"`; `export interface Producto { id: string; nombre: string; precio: number; talle: string; categoria: Categoria; descripcion: string; imagen_url: string; vendido: boolean; orden: number; creado_en: string; }`
  - `export const supabaseEnabled: boolean` (true si hay URL+key).
  - Funciones async: `listProductos(): Promise<Producto[]>`, `getProducto(id: string): Promise<Producto | null>`, `createProducto(data: NuevoProducto, file: File): Promise<void>`, `updateProducto(id: string, data: Partial<NuevoProducto>, file?: File): Promise<void>`, `deleteProducto(id: string): Promise<void>`, `toggleVendido(id: string, vendido: boolean): Promise<void>`, `statsAdmin(): Promise<{ activos: number; vendidosMes: number }>`.
  - `export type NuevoProducto = Omit<Producto, "id" | "creado_en" | "imagen_url"> & { imagen_url?: string }`
- Consumes: `theme.ts`/`config.ts` no; usa `import.meta.env`.

- [ ] **Step 1: `src/lib/supabase.ts`**

```ts
import { createClient } from "@supabase/supabase-js";

const url = import.meta.env.VITE_SUPABASE_URL as string | undefined;
const key = import.meta.env.VITE_SUPABASE_ANON_KEY as string | undefined;

export const supabaseEnabled = Boolean(url && key);

export const supabase = supabaseEnabled
  ? createClient(url!, key!)
  : (null as unknown as ReturnType<typeof createClient>);

export const BUCKET = "productos";
```

- [ ] **Step 2: `src/data/seed.ts` (6 productos demo)**

Usar imágenes libres del rubro (Unsplash, URLs directas). Precios como integer.
```ts
import type { Producto } from "../lib/products";

export const SEED: Producto[] = [
  { id: "seed-1", orden: 1, nombre: "Campera Bomber Vintage", precio: 45000, talle: "M", categoria: "VINTAGE", descripcion: "Campera bomber de colección, tela resistente y forro térmico.", imagen_url: "https://images.unsplash.com/photo-1551028719-00167b16eac5?w=800&q=80", vendido: false, creado_en: new Date().toISOString() },
  { id: "seed-2", orden: 2, nombre: "Jersey Retro Bulls 23", precio: 38000, talle: "L", categoria: "VINTAGE", descripcion: "Jersey de basketball estilo 90s, bordado original.", imagen_url: "https://images.unsplash.com/photo-1519861531473-9200262188bf?w=800&q=80", vendido: false, creado_en: new Date().toISOString() },
  { id: "seed-3", orden: 3, nombre: "Buzo Champion 90s", precio: 32000, talle: "M", categoria: "VINTAGE", descripcion: "Buzo con logo bordado, algodón grueso.", imagen_url: "https://images.unsplash.com/photo-1556821840-3a63f95609a7?w=800&q=80", vendido: false, creado_en: new Date().toISOString() },
  { id: "seed-4", orden: 4, nombre: "Remera NBA Classic", precio: 21000, talle: "S", categoria: "NUEVO", descripcion: "Remera 100% algodón con estampa NBA.", imagen_url: "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?w=800&q=80", vendido: false, creado_en: new Date().toISOString() },
  { id: "seed-5", orden: 5, nombre: "Gorra Snapback Retro", precio: 18000, talle: "Único", categoria: "VINTAGE", descripcion: "Gorra snapback de colección, visera plana.", imagen_url: "https://images.unsplash.com/photo-1588850561407-ed78c282e89b?w=800&q=80", vendido: false, creado_en: new Date().toISOString() },
  { id: "seed-6", orden: 6, nombre: "Short Basketball Vintage", precio: 24000, talle: "L", categoria: "VINTAGE", descripcion: "Short de basketball original de los 90, tela liviana y transpirable.", imagen_url: "https://images.unsplash.com/photo-1517466787929-bc90951d0974?w=800&q=80", vendido: false, creado_en: new Date().toISOString() },
];
```
(Si alguna URL de Unsplash no carga, reemplazar por otra del mismo rubro. El fallback visual de imagen se maneja en ProductCard.)

- [ ] **Step 3: `src/lib/products.ts` — tipos + funciones con fallback a seed**

```ts
import { supabase, supabaseEnabled, BUCKET } from "./supabase";
import { SEED } from "../data/seed";

export type Categoria = "VINTAGE" | "NUEVO";

export interface Producto {
  id: string;
  nombre: string;
  precio: number;
  talle: string;
  categoria: Categoria;
  descripcion: string;
  imagen_url: string;
  vendido: boolean;
  orden: number;
  creado_en: string;
}

export type NuevoProducto = Omit<Producto, "id" | "creado_en" | "imagen_url"> & { imagen_url?: string };

export async function listProductos(): Promise<Producto[]> {
  if (!supabaseEnabled) return [...SEED];
  const { data, error } = await supabase.from("productos").select("*").order("orden", { ascending: true });
  if (error) throw error;
  return (data ?? []) as Producto[];
}

export async function getProducto(id: string): Promise<Producto | null> {
  if (!supabaseEnabled) return SEED.find((p) => p.id === id) ?? null;
  const { data, error } = await supabase.from("productos").select("*").eq("id", id).maybeSingle();
  if (error) throw error;
  return (data as Producto) ?? null;
}

async function uploadImagen(file: File): Promise<string> {
  const ext = file.name.split(".").pop() || "jpg";
  const path = `${crypto.randomUUID()}.${ext}`;
  const { error } = await supabase.storage.from(BUCKET).upload(path, file, { upsert: false });
  if (error) throw error;
  const { data } = supabase.storage.from(BUCKET).getPublicUrl(path);
  return data.publicUrl;
}

export async function createProducto(data: NuevoProducto, file: File): Promise<void> {
  const imagen_url = await uploadImagen(file);
  const { error } = await supabase.from("productos").insert({ ...data, imagen_url });
  if (error) throw error;
}

export async function updateProducto(id: string, data: Partial<NuevoProducto>, file?: File): Promise<void> {
  const patch: Record<string, unknown> = { ...data };
  if (file) patch.imagen_url = await uploadImagen(file);
  const { error } = await supabase.from("productos").update(patch).eq("id", id);
  if (error) throw error;
}

export async function deleteProducto(id: string): Promise<void> {
  const { error } = await supabase.from("productos").delete().eq("id", id);
  if (error) throw error;
}

export async function toggleVendido(id: string, vendido: boolean): Promise<void> {
  const { error } = await supabase.from("productos").update({ vendido }).eq("id", id);
  if (error) throw error;
}

export async function statsAdmin(): Promise<{ activos: number; vendidosMes: number }> {
  const inicioMes = new Date();
  inicioMes.setDate(1);
  inicioMes.setHours(0, 0, 0, 0);
  const { count: activos } = await supabase.from("productos").select("*", { count: "exact", head: true }).eq("vendido", false);
  const { count: vendidosMes } = await supabase.from("productos").select("*", { count: "exact", head: true }).eq("vendido", true).gte("creado_en", inicioMes.toISOString());
  return { activos: activos ?? 0, vendidosMes: vendidosMes ?? 0 };
}
```

- [ ] **Step 4: `supabase/seed.sql` (schema + RLS + Storage + inserts)**

```sql
-- ============ M8 · schema + seed ============
-- Ejecutar en el SQL Editor de Supabase (una vez).

create table if not exists public.productos (
  id uuid primary key default gen_random_uuid(),
  nombre text not null,
  precio integer not null,
  talle text not null,
  categoria text not null check (categoria in ('VINTAGE','NUEVO')),
  descripcion text default '',
  imagen_url text default '',
  vendido boolean not null default false,
  orden integer not null default 0,
  creado_en timestamptz not null default now()
);
create index if not exists productos_orden_idx on public.productos (orden);

-- RLS: lectura pública, escritura solo autenticado
alter table public.productos enable row level security;
create policy "lectura publica" on public.productos for select using (true);
create policy "escritura autenticada" on public.productos for all to authenticated using (true) with check (true);

-- Storage bucket público para lectura
insert into storage.buckets (id, name, public) values ('productos','productos', true)
  on conflict (id) do nothing;
create policy "storage lectura publica" on storage.objects for select using (bucket_id = 'productos');
create policy "storage escritura autenticada" on storage.objects for insert to authenticated with check (bucket_id = 'productos');

-- Seed: 6 productos demo (editá libremente)
insert into public.productos (nombre, precio, talle, categoria, descripcion, imagen_url, orden) values
('Campera Bomber Vintage', 45000, 'M', 'VINTAGE', 'Campera bomber de colección, tela resistente y forro térmico.', 'https://images.unsplash.com/photo-1551028719-00167b16eac5?w=800&q=80', 1),
('Jersey Retro Bulls 23', 38000, 'L', 'VINTAGE', 'Jersey de basketball estilo 90s, bordado original.', 'https://images.unsplash.com/photo-1519861531473-9200262188bf?w=800&q=80', 2),
('Buzo Champion 90s', 32000, 'M', 'VINTAGE', 'Buzo con logo bordado, algodón grueso.', 'https://images.unsplash.com/photo-1556821840-3a63f95609a7?w=800&q=80', 3),
('Remera NBA Classic', 21000, 'S', 'NUEVO', 'Remera 100% algodón con estampa NBA.', 'https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?w=800&q=80', 4),
('Gorra Snapback Retro', 18000, 'Único', 'VINTAGE', 'Gorra snapback de colección, visera plana.', 'https://images.unsplash.com/photo-1588850561407-ed78c282e89b?w=800&q=80', 5),
('Short Basketball Vintage', 24000, 'L', 'VINTAGE', 'Short de basketball original de los 90, tela liviana y transpirable.', 'https://images.unsplash.com/photo-1517466787929-bc90951d0974?w=800&q=80', 6);
```

- [ ] **Step 5: Verificar build**

Run: `npm run build`
Expected: sin errores de TS (tipos de products.ts correctos).

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: capa de datos Supabase con fallback a seed + seed.sql"
```

---

### Task 3: Componentes de marca (HangTag + Icons)

**Files:**
- Create: `src/components/HangTag.tsx`, `src/components/Icons.tsx`

**Interfaces:**
- Produces:
  - `HangTag`: `export function HangTag(props: { w?: number; h?: number; fill: string; holeColor: string; rotate?: number }): JSX.Element`
  - `Icons.tsx`: named exports `IconArrowLeft, IconChat, IconPlus, IconPencil, IconTrash, IconCheck, IconPhone, IconMonitor, IconInstagram, IconPin` — cada uno `(props: { size?: number; color?: string }) => JSX.Element`.

- [ ] **Step 1: `HangTag.tsx`**

```tsx
export function HangTag({ w = 46, h = 20, fill, holeColor, rotate = 0 }: {
  w?: number; h?: number; fill: string; holeColor: string; rotate?: number;
}) {
  return (
    <svg width={w} height={h} viewBox="0 0 46 20" style={{ transform: `rotate(${rotate}deg)`, display: "block" }}>
      <path d="M1.5 2 H33 L44.5 10 L33 18 H1.5 Z" fill={fill} stroke="rgba(0,0,0,0.12)" strokeWidth="0.5" />
      <circle cx="9" cy="10" r="2.1" fill={holeColor} opacity="0.85" />
    </svg>
  );
}
```

- [ ] **Step 2: `Icons.tsx` — SVG stroke-based inline**

Todos con base `viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth={2} strokeLinecap="round" strokeLinejoin="round"`. Implementar helper interno `Base`:
```tsx
import type { ReactNode } from "react";

function Base({ size = 24, color = "currentColor", children }: { size?: number; color?: string; children: ReactNode }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke={color}
         strokeWidth={2} strokeLinecap="round" strokeLinejoin="round" style={{ display: "block" }}>
      {children}
    </svg>
  );
}

export const IconArrowLeft = (p: { size?: number; color?: string }) => (
  <Base {...p}><line x1="19" y1="12" x2="5" y2="12" /><polyline points="12 19 5 12 12 5" /></Base>
);
export const IconChat = (p: { size?: number; color?: string }) => (
  <Base {...p}><path d="M21 11.5a8.38 8.38 0 0 1-8.5 8.5 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7A8.38 8.38 0 0 1 4 11.5 8.5 8.5 0 0 1 12.5 3 8.5 8.5 0 0 1 21 11.5z" /></Base>
);
export const IconPlus = (p: { size?: number; color?: string }) => (
  <Base {...p}><line x1="12" y1="5" x2="12" y2="19" /><line x1="5" y1="12" x2="19" y2="12" /></Base>
);
export const IconPencil = (p: { size?: number; color?: string }) => (
  <Base {...p}><path d="M12 20h9" /><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4Z" /></Base>
);
export const IconTrash = (p: { size?: number; color?: string }) => (
  <Base {...p}><polyline points="3 6 5 6 21 6" /><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2" /></Base>
);
export const IconCheck = (p: { size?: number; color?: string }) => (
  <Base {...p}><polyline points="20 6 9 17 4 12" /></Base>
);
export const IconPhone = (p: { size?: number; color?: string }) => (
  <Base {...p}><rect x="5" y="2" width="14" height="20" rx="2" ry="2" /><line x1="12" y1="18" x2="12" y2="18" /></Base>
);
export const IconMonitor = (p: { size?: number; color?: string }) => (
  <Base {...p}><rect x="2" y="3" width="20" height="14" rx="2" ry="2" /><line x1="8" y1="21" x2="16" y2="21" /><line x1="12" y1="17" x2="12" y2="21" /></Base>
);
export const IconInstagram = (p: { size?: number; color?: string }) => (
  <Base {...p}><rect x="2" y="2" width="20" height="20" rx="5" ry="5" /><path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" /><line x1="17.5" y1="6.5" x2="17.5" y2="6.5" /></Base>
);
export const IconPin = (p: { size?: number; color?: string }) => (
  <Base {...p}><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z" /><circle cx="12" cy="10" r="3" /></Base>
);
```

- [ ] **Step 3: Verificar build**

Run: `npm run build`
Expected: sin errores.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: HangTag e íconos SVG inline"
```

---

### Task 4: Página Catálogo + ProductCard + rutas

**Files:**
- Create: `src/components/ProductCard.tsx`, `src/pages/Catalog.tsx`
- Modify: `src/App.tsx` (rutas reales)

**Interfaces:**
- Consumes: `listProductos`, `Producto` (Task 2); `HangTag`, `Icons` (Task 3); `C`, `font`, `eyebrow` (Task 1); `formatPrecio` (Task 1); config `INSTAGRAM_*`, `LOCATION`.
- Produces: `ProductCard({ p }: { p: Producto })`; ruta `/` renderiza `<Catalog/>`.

- [ ] **Step 1: `ProductCard.tsx`**

Comportamiento: contenedor con estado hover; al hover fondo BEIGE + `translateY(-3px)` con `transition: all .18s ease`. Imagen del producto siempre sobre fondo oscuro (`C.CARD`). Etiqueta HangTag arriba a la izquierda con texto VINTAGE/NUEVO superpuesto; talle; nombre; precio. Link a `/producto/:id`.
```tsx
import { useState } from "react";
import { Link } from "react-router-dom";
import type { Producto } from "../lib/products";
import { C, font, eyebrow } from "../theme";
import { formatPrecio } from "../lib/format";
import { HangTag } from "./HangTag";

export function ProductCard({ p }: { p: Producto }) {
  const [hover, setHover] = useState(false);
  const tagFill = p.categoria === "VINTAGE" ? C.BEIGE : C.SAGE;
  return (
    <Link to={`/producto/${p.id}`} style={{ textDecoration: "none" }}
      onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}>
      <div style={{
        background: hover ? C.BEIGE : C.CARD,
        borderRadius: 14, overflow: "hidden", fontFamily: font,
        transform: hover ? "translateY(-3px)" : "none",
        transition: "all .18s ease", border: `1px solid ${C.LINE}`,
      }}>
        <div style={{ position: "relative", background: C.INK, aspectRatio: "3/4" }}>
          <img src={p.imagen_url} alt={p.nombre}
            style={{ width: "100%", height: "100%", objectFit: "cover",
                     opacity: p.vendido ? 0.5 : 1, background: C.INK }} />
          <div style={{ position: "absolute", top: 10, left: 10, display: "flex", alignItems: "center" }}>
            <span style={{ position: "relative" }}>
              <HangTag w={64} h={26} fill={tagFill} holeColor={C.INK} />
              <span style={{ position: "absolute", inset: 0, display: "flex", alignItems: "center",
                paddingLeft: 20, fontSize: 9, fontWeight: 800, letterSpacing: "0.14em", color: C.INK }}>
                {p.categoria}
              </span>
            </span>
          </div>
          {p.vendido && (
            <div style={{ position: "absolute", top: 10, right: 10, background: C.INK, color: C.BEIGE,
              ...eyebrow, fontSize: 9, padding: "4px 8px", borderRadius: 6 }}>VENDIDO</div>
          )}
        </div>
        <div style={{ padding: "10px 12px 14px" }}>
          <div style={{ ...eyebrow, color: hover ? C.INK : C.MUTED_L, fontSize: 10 }}>TALLE {p.talle}</div>
          <div style={{ fontWeight: 700, fontSize: 14, color: hover ? C.INK : C.CREAM, marginTop: 3 }}>{p.nombre}</div>
          <div style={{ fontWeight: 900, fontSize: 16, color: hover ? C.INK : C.BEIGE, marginTop: 4, letterSpacing: "-0.5px" }}>
            {formatPrecio(p.precio)}
          </div>
        </div>
      </div>
    </Link>
  );
}
```

- [ ] **Step 2: `Catalog.tsx`** (hero + grid + footer; carga async con estado)

Estructura: hero fondo INK con logo circular (`/logo.png`, borde beige 3px), watermark `HangTag` grande detrás (opacity 0.05, rotate 18, absolute), eyebrow "M8 · BUENOS AIRES", h1 "Vintage & Streetwear" (900), subtítulo "Prendas Seleccionadas 🇺🇸". Grid responsive vía `grid-template-columns` con media query (usar `<style>` embebido para el breakpoint 768px con clases `.m8-grid`). Footer bloque CREAM + bloque BEIGE (Instagram link, pin+ubicación, "Envíos a todo el país 🇦🇷").
Incluir en el archivo un `<style>` con:
```css
.m8-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 14px; }
@media (min-width: 768px) { .m8-grid { grid-template-columns: repeat(3, 1fr); } }
```
Carga:
```tsx
const [productos, setProductos] = useState<Producto[]>([]);
const [loading, setLoading] = useState(true);
useEffect(() => { listProductos().then(setProductos).finally(() => setLoading(false)); }, []);
```
Render del footer Instagram:
```tsx
<a href={INSTAGRAM_URL} target="_blank" rel="noopener noreferrer"
   style={{ display: "inline-flex", alignItems: "center", gap: 8, color: C.INK, textDecoration: "none", fontWeight: 800 }}>
  <IconInstagram size={20} color={C.INK} /> {INSTAGRAM_USER}
</a>
```

- [ ] **Step 3: `App.tsx` con rutas reales**

```tsx
import { Routes, Route } from "react-router-dom";
import Catalog from "./pages/Catalog";
import Product from "./pages/Product";
import Admin from "./pages/Admin";

export default function App() {
  return (
    <Routes>
      <Route path="/" element={<Catalog />} />
      <Route path="/producto/:id" element={<Product />} />
      <Route path="/admin" element={<Admin />} />
    </Routes>
  );
}
```
(NOTA: `Product` y `Admin` se crean en Tasks 5–7. Para que compile ahora, crear stubs mínimos `export default function Product(){return null}` en sus archivos; se reemplazan en tareas siguientes.)

- [ ] **Step 4: Verificar build + visual**

Run: `npm run build` → sin errores.
Run: `npm run dev`, abrir `http://localhost:5173/`.
Expected: hero con logo/watermark, grid con 6 productos (imágenes del seed), hover de tarjeta pasa a beige y se eleva, footer con Instagram y ubicación. Responsive 2/3 columnas al cruzar 768px.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: pagina catalogo, ProductCard y rutas"
```

---

### Task 5: Página Producto individual

**Files:**
- Create: `src/components/SizeSelector.tsx`, `src/pages/Product.tsx` (reemplaza stub)

**Interfaces:**
- Consumes: `getProducto`, `Producto`; `whatsappUrl`, `formatPrecio`; `HangTag`, `IconArrowLeft`, `IconChat`; `C`, `font`, `eyebrow`.
- Produces: `SizeSelector({ talle }: { talle: string })`; ruta `/producto/:id` renderiza `<Product/>`.

- [ ] **Step 1: `SizeSelector.tsx`**

Solo informativo. Si `talle ∈ {XS,S,M,L,XL}` muestra los 5 con el disponible resaltado (fondo INK/texto BEIGE), resto en outline. Si no, muestra badge "Talle único".
```tsx
import { C, eyebrow } from "../theme";
const TALLES = ["XS", "S", "M", "L", "XL"];
export function SizeSelector({ talle }: { talle: string }) {
  if (!TALLES.includes(talle)) {
    return <div style={{ ...eyebrow, display: "inline-block", background: C.INK, color: C.BEIGE, padding: "8px 14px", borderRadius: 8 }}>Talle único</div>;
  }
  return (
    <div style={{ display: "flex", gap: 8 }}>
      {TALLES.map((t) => {
        const on = t === talle;
        return (
          <div key={t} style={{
            width: 44, height: 44, display: "flex", alignItems: "center", justifyContent: "center",
            borderRadius: 8, fontWeight: 800, fontSize: 13,
            background: on ? C.INK : "transparent", color: on ? C.BEIGE : C.MUTED,
            border: `1px solid ${on ? C.INK : C.LINE}`,
          }}>{t}</div>
        );
      })}
    </div>
  );
}
```

- [ ] **Step 2: `Product.tsx`**

Botón "Volver al catálogo" (`Link to="/"` + `IconArrowLeft`). Carga producto por `useParams().id`. Layout 2 columnas en desktop (imagen grande izquierda / info derecha), 1 columna mobile. Galería: estado `activa` (url); 3 miniaturas clickeables — como hay 1 imagen, repetir la misma 3 veces (miniaturas cambian `activa`, con esta imagen es la misma; deja el mecanismo listo si en el futuro hay varias). Etiqueta HangTag + ref `#M8-0{orden}`. Nombre, precio grande (900), descripción. `SizeSelector`. Botón "Consultar por WhatsApp" (`background: C.INK`, `color: C.BEIGE`, `IconChat`) → `href={whatsappUrl(p.nombre, formatPrecio(p.precio))}` target `_blank`.
Manejo not-found: si `getProducto` devuelve null, mostrar mensaje "Producto no encontrado" + link al catálogo.

- [ ] **Step 3: Verificar build + visual**

Run: `npm run build` → OK.
Run: `npm run dev`, click en una tarjeta.
Expected: detalle con imagen grande, miniaturas, ref `#M8-0X`, selector de talle (badge "Talle único" en la gorra), botón WhatsApp abre `wa.me/5493415447504?text=...` con nombre y precio.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: pagina de producto con selector de talle y WhatsApp"
```

---

### Task 6: Auth — Login + ruta protegida

**Files:**
- Create: `src/pages/Login.tsx`, `src/components/ProtectedRoute.tsx`, `src/lib/auth.ts`

**Interfaces:**
- Produces:
  - `auth.ts`: `useSession(): { session: Session | null; loading: boolean }` (hook con `supabase.auth.onAuthStateChange`), `signIn(email, password): Promise<{ error: string | null }>`, `signOut(): Promise<void>`.
  - `ProtectedRoute({ children }: { children: JSX.Element })` — si loading muestra spinner simple; si no hay sesión renderiza `<Login/>`; si hay sesión renderiza children.
- Consumes: `supabase`, `supabaseEnabled`.

- [ ] **Step 1: `lib/auth.ts`**

```ts
import { useEffect, useState } from "react";
import type { Session } from "@supabase/supabase-js";
import { supabase, supabaseEnabled } from "./supabase";

export function useSession() {
  const [session, setSession] = useState<Session | null>(null);
  const [loading, setLoading] = useState(true);
  useEffect(() => {
    if (!supabaseEnabled) { setLoading(false); return; }
    supabase.auth.getSession().then(({ data }) => { setSession(data.session); setLoading(false); });
    const { data: sub } = supabase.auth.onAuthStateChange((_e, s) => setSession(s));
    return () => sub.subscription.unsubscribe();
  }, []);
  return { session, loading };
}

export async function signIn(email: string, password: string): Promise<{ error: string | null }> {
  const { error } = await supabase.auth.signInWithPassword({ email, password });
  return { error: error ? error.message : null };
}

export async function signOut(): Promise<void> {
  await supabase.auth.signOut();
}
```

- [ ] **Step 2: `pages/Login.tsx`**

Form email/password sobre fondo INK, tarjeta CARD, botón BEIGE. Eyebrow con HangTag "PANEL DE ADMINISTRACIÓN". Maneja error de credenciales mostrando mensaje. Si `!supabaseEnabled`, mostrar aviso: "Configurá Supabase (.env) para habilitar el panel admin."

- [ ] **Step 3: `components/ProtectedRoute.tsx`**

```tsx
import type { JSX } from "react";
import { useSession } from "../lib/auth";
import { supabaseEnabled } from "../lib/supabase";
import Login from "../pages/Login";
import { C, font } from "../theme";

export function ProtectedRoute({ children }: { children: JSX.Element }) {
  const { session, loading } = useSession();
  if (!supabaseEnabled) return <Login />;
  if (loading) return <div style={{ minHeight: "100vh", background: C.INK, color: C.MUTED_L, fontFamily: font, display: "grid", placeItems: "center" }}>Cargando…</div>;
  if (!session) return <Login />;
  return children;
}
```

- [ ] **Step 4: Verificar build**

Run: `npm run build` → OK.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: auth Supabase, login y ruta protegida"
```

---

### Task 7: Panel Admin — stats, tabla, formulario, CRUD

**Files:**
- Create: `src/components/ProductForm.tsx`
- Create: `src/pages/Admin.tsx` (reemplaza stub)
- Modify: `src/App.tsx` (envolver `/admin` en `ProtectedRoute`)

**Interfaces:**
- Consumes: `listProductos, createProducto, updateProducto, deleteProducto, toggleVendido, statsAdmin, Producto, NuevoProducto, Categoria`; `signOut`; `ProtectedRoute`; `HangTag`, `IconPlus, IconPencil, IconTrash, IconCheck`; `C, font, eyebrow`; `formatPrecio`.
- Produces: `ProductForm({ initial, onDone, onCancel }: { initial?: Producto; onDone: () => void; onCancel: () => void })` — modal de alta/edición.

- [ ] **Step 1: `ProductForm.tsx`**

Modal (overlay fijo). Campos: foto (`<input type="file" accept="image/*">`, obligatoria en alta, opcional en edición), nombre, precio (number), talle (text), categoría (select VINTAGE/NUEVO), descripción (textarea). En submit: si `initial` → `updateProducto(initial.id, data, file?)`; si no → requiere file y `createProducto(data, file)`. Estado de guardando + manejo de error visible. Al terminar llama `onDone()`.
```tsx
// firma de estado del form
const [nombre, setNombre] = useState(initial?.nombre ?? "");
const [precio, setPrecio] = useState(initial?.precio ?? 0);
const [talle, setTalle] = useState(initial?.talle ?? "");
const [categoria, setCategoria] = useState<Categoria>(initial?.categoria ?? "VINTAGE");
const [descripcion, setDescripcion] = useState(initial?.descripcion ?? "");
const [orden, setOrden] = useState(initial?.orden ?? 0);
const [file, setFile] = useState<File | null>(null);
```
Payload: `const data: NuevoProducto = { nombre, precio, talle, categoria, descripcion, orden, vendido: initial?.vendido ?? false };`

- [ ] **Step 2: `Admin.tsx`**

Fondo CARD. Header: eyebrow con HangTag "PANEL DE ADMINISTRACIÓN" + botón salir (`signOut` → recarga a login). Dos tarjetas stats (fondo CARD2): "Productos activos" (`stats.activos`) y "Vendidos este mes" (`stats.vendidosMes`, con acento SAGE). Botón "+ Nuevo" (BEIGE, `IconPlus`) abre `ProductForm` sin `initial`. Tabla productos: miniatura, nombre, precio, estado (Disponible/Vendido), acciones — botón vendido/`IconCheck` (toggle), editar `IconPencil` (abre form con `initial`), eliminar `IconTrash` (confirm nativo + `deleteProducto`). Tras cualquier mutación: recargar `listProductos()` + `statsAdmin()`.
Estado:
```tsx
const [items, setItems] = useState<Producto[]>([]);
const [stats, setStats] = useState({ activos: 0, vendidosMes: 0 });
const [editing, setEditing] = useState<Producto | null | undefined>(undefined); // undefined=cerrado, null=nuevo, Producto=editar
async function reload() { setItems(await listProductos()); setStats(await statsAdmin()); }
useEffect(() => { reload(); }, []);
```
Eliminar: `if (confirm("¿Eliminar producto?")) { await deleteProducto(id); reload(); }`.

- [ ] **Step 3: `App.tsx` — proteger `/admin`**

```tsx
import { ProtectedRoute } from "./components/ProtectedRoute";
// ...
<Route path="/admin" element={<ProtectedRoute><Admin /></ProtectedRoute>} />
```

- [ ] **Step 4: Verificar build + visual (demo mode)**

Run: `npm run build` → OK.
Run: `npm run dev`, abrir `/admin`.
Expected (sin .env): muestra Login con aviso "Configurá Supabase…". Con `.env` real: login funciona, tabla lista productos, alta sube foto a Storage y aparece en catálogo, editar/eliminar/toggle vendido reflejan en `/`. Stats correctas.

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: panel admin con stats, tabla, formulario y CRUD"
```

---

### Task 8: Logo placeholder + README

**Files:**
- Create: `public/logo.png` (placeholder), `README.md`

**Interfaces:** ninguna nueva.

- [ ] **Step 1: Generar `public/logo.png` placeholder**

Crear un PNG circular simple con "M8" (script Node o un SVG→PNG). Alternativa mínima: guardar un SVG como `public/logo.png` no sirve (extensión). Usar un placeholder PNG 256×256. El cliente reemplazará el archivo con el logo real (mismo path).

- [ ] **Step 2: `README.md`**

Documentar: requisitos, `npm install && npm run dev`, modo demo vs Supabase, cómo crear el proyecto Supabase y correr `supabase/seed.sql`, dónde van las env (`.env` desde `.env.example`), cómo crear el usuario admin (Supabase Auth → Add user), y cómo reemplazar `public/logo.png` y el número de WhatsApp (`src/config.ts`).

- [ ] **Step 3: Verificar**

Run: `npm run dev` → catálogo muestra el logo (placeholder) en el hero sin 404.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: logo placeholder y README"
```

---

## Self-Review

**Spec coverage:**
- Stack/estilos/ruteo → Task 1, 4. ✅
- Tabla productos + Storage + Auth → Task 2, 6. ✅
- Paleta/tipografía/HangTag/Icons → Task 1, 3. ✅
- Catálogo (hero, grid, hover, footer) → Task 4. ✅
- Producto (galería, selector talle informativo, WhatsApp) → Task 5. ✅
- Admin (stats, tabla, form, CRUD, toggle vendido) → Task 7. ✅
- Config única (WhatsApp/IG/ubicación) → Task 1. ✅
- Seed 6 productos + seed.sql → Task 2. ✅
- Modo demo local sin Supabase → Task 2. ✅
- Fuera de scope (buscador/filtros/share/SEO) → no incluido. ✅
- Logo real path `public/logo.png` → Task 8 (placeholder hasta recibir archivo). ✅

**Placeholder scan:** sin TBD/TODO en pasos de código. Los stubs de Product/Admin en Task 4 son intencionales y se reemplazan en Tasks 5/7 (documentado).

**Type consistency:** `Producto`, `NuevoProducto`, `Categoria` definidos en Task 2 y usados consistentemente. Funciones de datos con firmas idénticas entre Interfaces y uso. `useSession`/`signIn`/`signOut` consistentes entre Task 6 y 7.
